{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Example Claude Code settings. Copy to ~/.claude/settings.json and adjust. Contains NO secrets — credentials live in ~/.claude.json / your keychain, which agent-home never touches.",
  "includeCoAuthoredBy": true
}
